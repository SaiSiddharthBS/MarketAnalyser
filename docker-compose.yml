

services:
  market-analyser:
    build: .
    container_name: agent_alpha_bot
    restart: unless-stopped
    volumes:
      - ./backend/data:/app/data
    environment:
      - TZ=Asia/Kolkata
      # Add API keys here if needed
      # - FRED_API_KEY=your_key
      # - TELEGRAM_BOT_TOKEN=your_token
      # - TELEGRAM_CHAT_ID=your_id
