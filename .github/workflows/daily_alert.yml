name: Agent Alpha Daily Briefing

on:
  schedule:
    # Runs at 2:30 AM UTC (8:00 AM IST) Monday to Friday
    - cron: '30 2 * * 1-5'
    # Runs at 10:45 AM UTC (4:15 PM IST) Monday to Friday
    - cron: '45 10 * * 1-5'
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  send-alert:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install fastapi "uvicorn[standard]" yfinance requests beautifulsoup4 pandas numpy ta vaderSentiment feedparser python-telegram-bot scikit-learn xgboost lightgbm joblib mftool
        
    - name: Run Daily Job
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        python backend/bot/daily_job.py
